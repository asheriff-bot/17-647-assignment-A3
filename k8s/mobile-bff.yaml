apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobile-bff
  namespace: bookstore-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mobile-bff
  template:
    metadata:
      labels:
        app: mobile-bff
    spec:
      containers:
        - name: mobile-bff
          image: YOUR_REGISTRY/mobile-bff:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: URL_BASE_BACKEND_SERVICES
              value: http://backend-router:3000
            - name: PORT
              value: "80"
          livenessProbe:
            httpGet:
              path: /status
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: mobile-bff
  namespace: bookstore-ns
spec:
  type: LoadBalancer
  selector:
    app: mobile-bff
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
